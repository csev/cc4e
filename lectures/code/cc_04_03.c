
int main(void)
{
    struct Map * map = Map_new();
    struct MapEntry *cur;

    printf("Map test\n");
    map->put(map, "z", 8);
    map->put(map, "z", 1);
    map->put(map, "y", 2);
    map->put(map, "b", 3);
    map->put(map, "a", 4);
    map->dump(map);

    printf("size=%d\n", map->size(map));

    printf("z=%d\n", map->get(map, "z", 42));
    printf("x=%d\n", map->get(map, "x", 42));

    // No iterator (for now)
 
    map->del(map);
}

// This does not compile - it needs a bunch of library code
